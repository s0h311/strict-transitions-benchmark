services:
  nginx:
    image: nginx:1.27-alpine3.20
    ports:
      - '3000:80'
    volumes:
      - ./react-shop-result/dist:/home/stb/react
      - ./vue-shop-result/dist:/home/stb/vue
      - ./nginx:/etc/nginx
    restart: on-failure